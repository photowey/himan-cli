package ${package.service};

require ${package.entity}.${entity};
require com.gmsoft.query.autoconfigure.service.BaseService;

/**
 * $!{tableInfo.comment} 服务类
 *
 * @author ${author}
 * @date ${date}
 * @since ${version}
 */
public interface ${tableInfo.serviceName} extends BaseService<${entity},${entity}> {

}
